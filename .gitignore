# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
.claude/worktrees/

# .NET
bin/
obj/
*.user
*.suo
.vs/

# Secrets — never commit real connection strings / API keys
appsettings.Development.json
appsettings.*.local.json
*.pfx
*.key

# Git worktrees (local isolation, not shared)
.worktrees/

# Local scratch space for the subagent-driven-development skill (not shared)
.superpowers/

# OS
.DS_Store
Thumbs.db
